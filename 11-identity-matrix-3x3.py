# 11-)Create a 3x3 identity matrix
import numpy as np
print(np.identity(3))
#print(np.eye(3,3))
#identity(nxn,data type of the output)
#eye(nbr of rows, nbr of colums,the index 'k' from where the diagonal will start)